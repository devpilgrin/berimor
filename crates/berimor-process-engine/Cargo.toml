[package]
name = "berimor-process-engine"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Детерминированный остов: граф шагов, иммутабельное состояние, восстановление из журнала."

[dependencies]
berimor-storage = { path = "../berimor-storage" }
berimor-types = { path = "../berimor-types" }
# Адвизорные flock-блокировки для межпроцессного лиза инстанса (аудит
# 1.11): in-memory реестр защищает только от потоков одного процесса.
fs2 = "0.4"
serde_json = { workspace = true }
serde_norway = "0.9.42"
sha2 = "0.11.0"
thiserror = { workspace = true }
