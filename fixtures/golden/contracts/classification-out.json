{
  "$comment": "Дословный пример контракта из docs/arch/mediation.md §3. ROADMAP: M1, M3. Версия 2 (0.30.0, issue #4): обязательное поле-обоснование risk_factors ПЕРЕД целевым risk — порядок объявления = порядок в схеме и генерации (SGR).",
  "name": "ClassificationOut",
  "schema_version": 2,
  "deny_unknown_fields": true,
  "fields": {
    "category": { "type": "enum", "values": ["billing", "card", "debt", "other"], "required": true },
    "risk_factors": { "type": "array", "items": "string", "min_items": 1, "required": true },
    "risk": { "type": "integer", "min": 0, "max": 10, "required": true },
    "summary": { "type": "string", "max_length": 280, "required": true }
  },
  "policy": [
    "risk >= 7 => category != 'other'"
  ]
}
