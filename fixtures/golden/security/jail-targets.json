{
  "$comment": "Золотой набор целей для интеграции FsJail в capability-гейт (ROADMAP S2/§20.3). Тест строит временную структуру: root/sub/file.txt, root/outlink -> <внешний каталог>, и прогоняет каждую цель через StandardCapability::check с реальным jail. 'denied' обязаны дать CapabilityDecision::Deny (нарушение jail — безусловный запрет во всех режимах, security-model.md §3); 'allowed' НЕ обязаны быть Deny (могут требовать подтверждения по режиму — другой слой). Пути в кейсах — относительно корня jail; {OUTSIDE} — маркер внешнего каталога, подставляется тестом.",
  "denied": [
    { "name": "выход через .. к внешнему файлу", "path": "../outside/secret.txt" },
    { "name": "абсолютный путь вне области", "path": "{OUTSIDE}/secret.txt" },
    { "name": "симлинк, указывающий наружу", "path": "outlink/secret.txt" },
    { "name": "несуществующий лист под ускользающим симлинком", "path": "outlink/new-file.txt" },
    { "name": "системный файл абсолютным путём", "path": "/etc/passwd" },
    { "name": "симлинк + .. — на POSIX ядро резолвит симлинк раньше следующего компонента (C1 независимого ревью S2); jail отклоняет на всех платформах, т.к. симлинк наружу встречается до '..' и канонизируется с проверкой", "path": "outlink/../outside/secret.txt" }
  ],
  "allowed": [
    { "name": "существующий файл внутри области", "path": "sub/file.txt" },
    { "name": "несуществующий лист внутри — законная цель записи", "path": "sub/new-file.txt" },
    { "name": "симлинк, указывающий внутрь области", "path": "sublink/file.txt" },
    { "name": ".. после симлинка, указывающего ВНУТРЬ — физически внутри, легально", "path": "sublink/../sub/file.txt" },
    { "name": ".. после несуществующего компонента — лексический подъём, статически разрешим на всех платформах (POSIX даст ENOENT при обращении, выхода наружу нет)", "path": "nonexistent/../sub/file.txt" },
    { "name": "сам корень области", "path": "." },
    { "name": "не-путь (идентификатор) трактуется как относительный путь внутри", "path": "card_1029" }
  ]
}
